"""
EOD / business-date / daily engine package.

This package owns:
- End-of-day orchestration and persistence (`eod.core`)
- EOD audit single-flight + run logging (`eod.audit`)
- Daily loan math engine (`eod.loan_daily_engine`)
- System business date config + tick (`eod.system_business_date`)
"""

